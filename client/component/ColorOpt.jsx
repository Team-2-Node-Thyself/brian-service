import React from 'react';
import style from 'styled-components';

var ColorOpt = (props) => {
    return (
        <div>This is where the color options will show up</div>
    );
};

export default ColorOpt;